@using DevsTutorialCenterMVC.Utilities
@model DevsTutorialCenterMVC.Models.Api.BlogPostVM;
@*@model BlogPostItemVM;

<div class="">
    <img class="aspect-[16/10] w-full object-cover object-center" src="@Model.ImageUrl"/>
    <div class="p-4">
        <div class="">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <div class="rounded-full bg-gray-100 px-3 py-1 text-sm uppercase text-slate-500">@Model.TagName</div>
                    <div class="w-4 border-t border-gray-300"></div>
                    <div class="text-gray-700">@Model.ReadTime read</div>
                </div>
            </div>
        </div>
        <div class="mt-2 text-xl font-semibold">
            @Model.Title
        </div>
        <div class="mt-2">@Model.Text[..50]</div>
        <div class="my-3 border-t border-gray-100"></div>
        <div class="flex items-center justify-between">
            <div class="flex items-center">
                <img class="mr-2 aspect-square h-10 rounded-full text-gray-900" src="@Model.AuthorImage"/>
                <h5>@Model.AuthorName</h5>
            </div>
            <p class="text-gray-400">@Model.CreatedOn.FormatDate()</p>
        </div>
    </div>
</div>*@
@model StoryPageVM;

<div class="flex flex-col justify-start items-start bg-gray-50 border border-red-600 p-10 md:flex-row md:space-x-10">
    <div class="flex flex-col space-y-8 border border-red-600 md:w-3/4">
        @foreach (var pendingArticle in Model.PendingArticles)
        {
            <partial name="Components/_PendingListArticlePartial" model="pendingArticle" />
        }
    </div>
</div>

